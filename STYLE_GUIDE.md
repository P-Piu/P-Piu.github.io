# P-Piu 的光合作用空间：样式约定

这份文件只供网站维护使用，不会发布到线上。

## 核心视觉

- 整体保持简洁、干净、克制，不使用炫彩渐变和多余装饰。
- 绿色统一使用固定色板，不再临时增加相近色号：
  - 品牌黄绿 `#B6D331`：顶部横条。
  - 交互黄绿 `#829B0E`：链接、悬停、选中状态和少量强调；与品牌色同色相，但在白底上更沉稳。
  - 深绿 `#315A3F`：辅助强调和较柔和的绿色文字。
  - 墨绿 `#1F3527`：主标题、栏目标题、卡片标题、顶部文字与图标。
  - 浅绿背景 `#F2F7F3`：卡片悬停等轻量背景。
  - 浅绿边线 `#DBE9DF`：卡片与分隔线。
- 正文以白色背景和深绿色文字为主。
- 黄绿色用于当前状态、入口链接和少量强调，不大面积铺陈。

## 左右导航层级

- 左侧站点名与右侧“目录”使用同一格式：`0.84rem`、`700`、`#1F3527`。
- 左右两侧一级栏目统一使用：`0.78rem`、`700`、`#3F4B43`。
- 左右两侧二级栏目统一使用：`0.66rem`、`400`、`#687169`，行高 `1.55`。
- 一级、二级栏目全部左对齐；不使用圆点或额外缩进。
- 二级栏目彼此之间、二级与一级之间保留清楚的纵向间距。
- 所有一级栏目保持相同的点击逻辑与交互样式；鼠标悬停和当前选中状态统一使用交互黄绿 `#829B0E`。
- 右侧目录顶部遮罩只在目录向下滚动后出现；位于顶部时不显示。

## 首页入口

- 桌面端五个入口保持同一行、同高，底部链接对齐。
- 说明文字不拆词，不留下只有零星几个字的末行。
- 窄屏使用横向滑动，避免强行压缩。

## 内容与排版

- 全站字号统一使用固定层级：辅助小字 `0.66rem`、次级文字 `0.78rem`、小标题 `0.9rem`、强调文案 `1.1rem`、页面主标题 `1.6rem`；首页网站名独立使用响应式展示字号。
- 普通页面标题统一为：H1 `1.6rem`、H2 `1.1rem`、H3 `0.9rem`。
- 用户提供的成稿默认锁定文字，不改字、换词或标点；只调整段落、换行、留白和层级。
- 只有用户明确要求润色时才修改措辞。
- 私人分享默认只用于理解和概括，除非用户明确允许，否则不写入公开网站。
- 同一页面的正文段落统一使用句末标点；标题和单独链接不加句号。
- 文章索引保持朴素的列表感，不使用整块悬停填色；以分隔线和“阅读全文 →”提示入口。
- 项目索引必须明确标注“状态：”，状态词统一使用“还没开始”“正在生长”“已完成”。
